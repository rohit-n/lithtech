before_script:
  - apt-get -qq update && apt-get install -y -qq cmake make lsb-release
  - lsb_release -a
  - g++ --version
  - clang --version
  - cmake --version

stages:
  - test

gcc 7:
  image: "gcc:7"
  stage: test
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make
clang 5:
  image: "clang:5"
  stage: test
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make
